import { ILocation } from './ilocation';

export interface IDestination {
    name: String;
    url: String;
    image_url: String;
    rating: Number;
    price: String;
    phone: String;
    id: String;
    is_closed: Boolean;
}
